dist: xenial
matrix:
    include:
        - language: python
          python: 3.7
          install: pip install -r backend/requirements.txt
          script:
              - cd backend
              - coverage run --source=swpp manage.py test
          after_success:
              - coveralls
